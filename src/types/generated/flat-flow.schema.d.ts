/* eslint-disable */
/**
 * This file was automatically generated by json-schema-to-typescript.
 * DO NOT MODIFY IT BY HAND. Instead, modify the source JSONSchema file,
 * and run json-schema-to-typescript to regenerate this file.
 */

/**
 * This interface was referenced by `undefined`'s JSON-Schema definition
 * via the `patternProperty` "^[a-z][a-z0-9_]*(\.[a-z][a-z0-9_]*)*$".
 */
export type FlatNode =
  | ProviderStepNode
  | GuardianStepNode
  | GateStepNode
  | SystemStepNode
  | TerminalNode
  | ParallelGroup;
export type FormField = {
  [k: string]: unknown;
} & {
  name: string;
  kind: "text" | "textarea" | "select" | "checkbox";
  required?: boolean;
  label?: string;
  /**
   * Required when kind=select.
   */
  options?: string[];
};

/**
 * Loader output. All macros are expanded; engine consumes only this. Identical structure to flow.json minus macro nodes plus parallel_group. Used as the single source of truth at engine compile time (XState machine generation).
 */
export interface CompiledFlatFlow {
  flow: string;
  version: 1;
  /**
   * RFC 3339 / ISO 8601 datetime, UTC preferred.
   */
  compiled_at: string;
  /**
   * Original flow.yaml path; for diagnostics only.
   */
  source_path?: string;
  defaults?: Defaults;
  variants: {
    [k: string]: Variant;
  };
  nodes: {
    [k: string]: FlatNode;
  };
  /**
   * Optional map: expanded_node_id → originating_macro_node_id. For diagnostics, audit, and 'inspect-flow' rendering. Engine ignores.
   */
  macro_origins?: {
    /**
     * Lower-case dotted path. Dots indicate parent.child relationship (e.g. parallel-group member). Underscores join words within a segment.
     *
     * This interface was referenced by `undefined`'s JSON-Schema definition
     * via the `patternProperty` "^[a-z][a-z0-9_]*$".
     *
     * This interface was referenced by `undefined`'s JSON-Schema definition
     * via the `patternProperty` "^[a-z][a-z0-9_]*(\.[a-z][a-z0-9_]*)*$".
     */
    [k: string]: string;
  };
}
export interface Defaults {
  timeout_minutes?: number;
  idle_timeout_minutes?: number;
  max_attempts?: number;
  max_rounds?: number;
}
/**
 * This interface was referenced by `undefined`'s JSON-Schema definition
 * via the `patternProperty` "^[a-z][a-z0-9_]*$".
 */
export interface Variant {
  /**
   * Lower-case dotted path. Dots indicate parent.child relationship (e.g. parallel-group member). Underscores join words within a segment.
   *
   * This interface was referenced by `undefined`'s JSON-Schema definition
   * via the `patternProperty` "^[a-z][a-z0-9_]*$".
   *
   * This interface was referenced by `undefined`'s JSON-Schema definition
   * via the `patternProperty` "^[a-z][a-z0-9_]*(\.[a-z][a-z0-9_]*)*$".
   */
  initial: string;
  label?: string;
}
export interface ProviderStepNode {
  type: "provider_step";
  /**
   * Subprocess CLI provider. New providers added here as the matrix grows.
   */
  provider: "claude" | "codex";
  role?: string;
  prompt?: PromptSpec;
  /**
   * Either a NodeId (uniform target) or a variant-keyed map { full: 'a', light: 'b' }.
   */
  on_done?:
    | string
    | {
        /**
         * Lower-case dotted path. Dots indicate parent.child relationship (e.g. parallel-group member). Underscores join words within a segment.
         *
         * This interface was referenced by `undefined`'s JSON-Schema definition
         * via the `patternProperty` "^[a-z][a-z0-9_]*$".
         *
         * This interface was referenced by `undefined`'s JSON-Schema definition
         * via the `patternProperty` "^[a-z][a-z0-9_]*(\.[a-z][a-z0-9_]*)*$".
         */
        [k: string]: string;
      };
  /**
   * Either a NodeId (uniform target) or a variant-keyed map { full: 'a', light: 'b' }.
   */
  on_failure?:
    | string
    | {
        /**
         * Lower-case dotted path. Dots indicate parent.child relationship (e.g. parallel-group member). Underscores join words within a segment.
         *
         * This interface was referenced by `undefined`'s JSON-Schema definition
         * via the `patternProperty` "^[a-z][a-z0-9_]*$".
         *
         * This interface was referenced by `undefined`'s JSON-Schema definition
         * via the `patternProperty` "^[a-z][a-z0-9_]*(\.[a-z][a-z0-9_]*)*$".
         */
        [k: string]: string;
      };
  timeout_minutes?: number;
  max_attempts?: number;
  label?: string;
  summary?: string;
  viewer_hint?: string;
  /**
   * Lower-case dotted path. Dots indicate parent.child relationship (e.g. parallel-group member). Underscores join words within a segment.
   */
  resume_session_from?: string;
  /**
   * F-012 (in-step turn loop): when true, this provider_step uses the provider-step-output envelope (kind: final | ask). On `ask`, the engine writes a turn-question.json and polls for turn-answer.json before resuming the provider session. Default false — provider output is unconstrained free text and the step is a single-shot.
   */
  enable_user_input?: boolean;
}
/**
 * Prompt-author hints. Engine extends this with system context (current state, evidence refs) when invoking the provider. Open shape on purpose: prompts are domain-specific.
 */
export interface PromptSpec {
  intent?: string;
  note_section?: string;
  commit_summary?: string;
  checkpoints?: string[];
  [k: string]: unknown;
}
export interface GuardianStepNode {
  type: "guardian_step";
  /**
   * Subprocess CLI provider. New providers added here as the matrix grows.
   */
  provider: "claude" | "codex";
  role?: string;
  /**
   * Reviewer / source nodes whose output the guardian must read. Single id or list.
   */
  inputs_from?: string | [string, ...string[]];
  outputs: GuardianOutputs;
  max_rounds?: number;
  label?: string;
  summary?: string;
}
/**
 * Maps each guardian decision value to a transition target. `pass` is mandatory; the rest are optional but at least one of {repair_needed, abort, escalate_human} should be wired so non-pass decisions resolve.
 */
export interface GuardianOutputs {
  /**
   * Either a NodeId (uniform target) or a variant-keyed map { full: 'a', light: 'b' }.
   */
  pass:
    | string
    | {
        /**
         * Lower-case dotted path. Dots indicate parent.child relationship (e.g. parallel-group member). Underscores join words within a segment.
         *
         * This interface was referenced by `undefined`'s JSON-Schema definition
         * via the `patternProperty` "^[a-z][a-z0-9_]*$".
         *
         * This interface was referenced by `undefined`'s JSON-Schema definition
         * via the `patternProperty` "^[a-z][a-z0-9_]*(\.[a-z][a-z0-9_]*)*$".
         */
        [k: string]: string;
      };
  repair_needed?: RepairTransition;
  /**
   * Either a NodeId (uniform target) or a variant-keyed map { full: 'a', light: 'b' }.
   */
  abort?:
    | string
    | {
        /**
         * Lower-case dotted path. Dots indicate parent.child relationship (e.g. parallel-group member). Underscores join words within a segment.
         *
         * This interface was referenced by `undefined`'s JSON-Schema definition
         * via the `patternProperty` "^[a-z][a-z0-9_]*$".
         *
         * This interface was referenced by `undefined`'s JSON-Schema definition
         * via the `patternProperty` "^[a-z][a-z0-9_]*(\.[a-z][a-z0-9_]*)*$".
         */
        [k: string]: string;
      };
  /**
   * Either a NodeId (uniform target) or a variant-keyed map { full: 'a', light: 'b' }.
   */
  escalate_human?:
    | string
    | {
        /**
         * Lower-case dotted path. Dots indicate parent.child relationship (e.g. parallel-group member). Underscores join words within a segment.
         *
         * This interface was referenced by `undefined`'s JSON-Schema definition
         * via the `patternProperty` "^[a-z][a-z0-9_]*$".
         *
         * This interface was referenced by `undefined`'s JSON-Schema definition
         * via the `patternProperty` "^[a-z][a-z0-9_]*(\.[a-z][a-z0-9_]*)*$".
         */
        [k: string]: string;
      };
}
export interface RepairTransition {
  /**
   * Lower-case dotted path. Dots indicate parent.child relationship (e.g. parallel-group member). Underscores join words within a segment.
   *
   * This interface was referenced by `undefined`'s JSON-Schema definition
   * via the `patternProperty` "^[a-z][a-z0-9_]*$".
   *
   * This interface was referenced by `undefined`'s JSON-Schema definition
   * via the `patternProperty` "^[a-z][a-z0-9_]*(\.[a-z][a-z0-9_]*)*$".
   */
  next: string;
  /**
   * Lower-case dotted path. Dots indicate parent.child relationship (e.g. parallel-group member). Underscores join words within a segment.
   *
   * This interface was referenced by `undefined`'s JSON-Schema definition
   * via the `patternProperty` "^[a-z][a-z0-9_]*$".
   *
   * This interface was referenced by `undefined`'s JSON-Schema definition
   * via the `patternProperty` "^[a-z][a-z0-9_]*(\.[a-z][a-z0-9_]*)*$".
   */
  max_round_escalation?: string;
}
export interface GateStepNode {
  type: "gate_step";
  approver_role?: string;
  show?: (
    | "diff"
    | "judgement_summary"
    | "guardian_reasoning"
    | "evidence"
    | "blocking_findings"
    | "git_log"
    | "related_tickets"
  )[];
  outputs: {
    /**
     * Either a NodeId (uniform target) or a variant-keyed map { full: 'a', light: 'b' }.
     */
    approved?:
      | string
      | {
          /**
           * Lower-case dotted path. Dots indicate parent.child relationship (e.g. parallel-group member). Underscores join words within a segment.
           *
           * This interface was referenced by `undefined`'s JSON-Schema definition
           * via the `patternProperty` "^[a-z][a-z0-9_]*$".
           *
           * This interface was referenced by `undefined`'s JSON-Schema definition
           * via the `patternProperty` "^[a-z][a-z0-9_]*(\.[a-z][a-z0-9_]*)*$".
           */
          [k: string]: string;
        };
    /**
     * Either a NodeId (uniform target) or a variant-keyed map { full: 'a', light: 'b' }.
     */
    rejected?:
      | string
      | {
          /**
           * Lower-case dotted path. Dots indicate parent.child relationship (e.g. parallel-group member). Underscores join words within a segment.
           *
           * This interface was referenced by `undefined`'s JSON-Schema definition
           * via the `patternProperty` "^[a-z][a-z0-9_]*$".
           *
           * This interface was referenced by `undefined`'s JSON-Schema definition
           * via the `patternProperty` "^[a-z][a-z0-9_]*(\.[a-z][a-z0-9_]*)*$".
           */
          [k: string]: string;
        };
    /**
     * Either a NodeId (uniform target) or a variant-keyed map { full: 'a', light: 'b' }.
     */
    cancelled?:
      | string
      | {
          /**
           * Lower-case dotted path. Dots indicate parent.child relationship (e.g. parallel-group member). Underscores join words within a segment.
           *
           * This interface was referenced by `undefined`'s JSON-Schema definition
           * via the `patternProperty` "^[a-z][a-z0-9_]*$".
           *
           * This interface was referenced by `undefined`'s JSON-Schema definition
           * via the `patternProperty` "^[a-z][a-z0-9_]*(\.[a-z][a-z0-9_]*)*$".
           */
          [k: string]: string;
        };
  };
  form?: FormField[];
  label?: string;
  summary?: string;
}
export interface SystemStepNode {
  type: "system_step";
  action: "close_ticket" | "release_lease" | "cleanup_worktree" | "barrier" | "noop";
  params?: {};
  /**
   * Either a NodeId (uniform target) or a variant-keyed map { full: 'a', light: 'b' }.
   */
  on_done:
    | string
    | {
        /**
         * Lower-case dotted path. Dots indicate parent.child relationship (e.g. parallel-group member). Underscores join words within a segment.
         *
         * This interface was referenced by `undefined`'s JSON-Schema definition
         * via the `patternProperty` "^[a-z][a-z0-9_]*$".
         *
         * This interface was referenced by `undefined`'s JSON-Schema definition
         * via the `patternProperty` "^[a-z][a-z0-9_]*(\.[a-z][a-z0-9_]*)*$".
         */
        [k: string]: string;
      };
  /**
   * Either a NodeId (uniform target) or a variant-keyed map { full: 'a', light: 'b' }.
   */
  on_failure?:
    | string
    | {
        /**
         * Lower-case dotted path. Dots indicate parent.child relationship (e.g. parallel-group member). Underscores join words within a segment.
         *
         * This interface was referenced by `undefined`'s JSON-Schema definition
         * via the `patternProperty` "^[a-z][a-z0-9_]*$".
         *
         * This interface was referenced by `undefined`'s JSON-Schema definition
         * via the `patternProperty` "^[a-z][a-z0-9_]*(\.[a-z][a-z0-9_]*)*$".
         */
        [k: string]: string;
      };
  label?: string;
}
export interface TerminalNode {
  type: "terminal";
  outcome: "success" | "aborted" | "needs_human";
  reason?: string;
}
export interface ParallelGroup {
  type: "parallel_group";
  /**
   * Node ids of parallel children. Each must exist as a separate node in the flow's nodes map (typically provider_step). Members must NOT define their own on_done; the group's on_all_done is implicit for them.
   *
   * @minItems 1
   */
  members: [string, ...string[]];
  /**
   * Either a NodeId (uniform target) or a variant-keyed map { full: 'a', light: 'b' }.
   */
  on_all_done:
    | string
    | {
        /**
         * Lower-case dotted path. Dots indicate parent.child relationship (e.g. parallel-group member). Underscores join words within a segment.
         *
         * This interface was referenced by `undefined`'s JSON-Schema definition
         * via the `patternProperty` "^[a-z][a-z0-9_]*$".
         *
         * This interface was referenced by `undefined`'s JSON-Schema definition
         * via the `patternProperty` "^[a-z][a-z0-9_]*(\.[a-z][a-z0-9_]*)*$".
         */
        [k: string]: string;
      };
  label?: string;
}
