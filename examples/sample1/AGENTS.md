# sample1 Agent Notes

- Keep implementation edits scoped to `src/calc_demo/`, `current-note.md`, and `current-ticket.md` unless the ticket clearly requires more.
- Use `scripts/test-all.sh` for verification.
- Do not edit `.pdh-flow/`, `tickets/`, or `.env`.
- For PD-C commits, use subjects like `[PD-C-6] Implement calc multiplication`.
