/* eslint-disable */
/**
 * This file was automatically generated by json-schema-to-typescript.
 * DO NOT MODIFY IT BY HAND. Instead, modify the source JSONSchema file,
 * and run json-schema-to-typescript to regenerate this file.
 */

/**
 * Structured output produced by the `final_verifier` provider_step. Replaces the previous practice of writing only a markdown `## final_verification` section to current-note.md and having close_gate parse the AC table back out of that markdown. The engine reads ac_verification[] directly from the judgement sidecar — never re-parses the prose.
 */
export interface FinalVerifierOutput {
  /**
   * One entry per Acceptance Criterion in the ticket. Order matches the ticket's AC list. The engine reads .status to drive close_gate's deferral_approvals requirement (any `unverified` row demands a deferral plan).
   *
   * @maxItems 50
   */
  ac_verification: AcRow[];
  /**
   * Full human-readable verification narrative — the same prose that previously made up the `## final_verification (round N)` section in current-note.md. Rendered as-is when surfaced; the FE never re-parses this string for data. The summary should still include an AC verification table (humans read it), but the table is presentation only — the engine source of truth is `ac_verification[]`.
   */
  summary_md: string;
}
export interface AcRow {
  /**
   * Verbatim text of the AC bullet from current-ticket.md `## Acceptance Criteria`.
   */
  ac_item: string;
  /**
   * Verification class carried forward from the assist / plan section.
   */
  class: "unit-test-sufficient" | "integration-required" | "real-env-required";
  /**
   * Verification verdict. `verified` requires concrete evidence (file path in `evidence_path`); `unverified` MUST be paired with a deferral plan at close_gate (`deferral_approvals` in the gate decision); `user_accepted` is reserved for Epic-level review and rarely appears here.
   */
  status: "verified" | "unverified" | "user_accepted";
  /**
   * Relative path (from worktree root) to the evidence file that proves this AC. Omit when status is `unverified` / `user_accepted`.
   */
  evidence_path?: string;
  /**
   * One-line context for the row — why unverified / what was checked / what gap remains. Surfaced to the close_gate human.
   */
  note?: string;
}
