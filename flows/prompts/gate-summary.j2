You assist a PdM who is about to approve or reject the gate `{{ nodeId }}` (round {{ round }}).

Read the three documents below and produce a **decision-support summary** in **Markdown** that the PdM can read in seconds.

**Hard constraints:**
- Output is **Markdown** with the section headings shown below, in this exact order.
- Total length **must be 30 lines or fewer**, including blank lines and headings.
- Detect the **language** used in the ticket and product brief. If any non-English language is present in either, write the **entire output in that language** (matching the ticket's language). If both ticket and brief are English-only, write in English.
- No preamble. No "Here is the summary". No closing remarks. Just the four sections below.
- If a document is missing or insufficient to fill a section, say so explicitly in that section — do not invent content.
- Stay concrete. Reference what is in the documents. Do not speculate beyond them.

**Required sections (use these exact headings):**

```
## 要約 / Summary
1–2 lines: what this gate is asking the PdM to decide.

## 達成したこと / What was done
Bullet list (≤6): concrete deliverables, decisions, or changes recorded in the note.

## 注意点 / Concerns
Bullet list (≤6): reviewer findings, residual risks, out-of-scope items, anything that could affect approval.

## 推奨判断 / Recommendation
1–2 lines: approve / reject (or その他) plus a one-line rationale tied to the documents above.
```

(Use the heading whose language matches the chosen output language. If you write in Japanese, use the Japanese half; if in English, use the English half. Do not include both.)

---

## Source documents

### 元のチケット / Ticket (current-ticket.md)
{% if ticket %}
{{ ticket }}
{% else %}
_(ticket file not found)_
{% endif %}

### 現在のノート / Note (current-note.md)
{% if note %}
{{ note }}
{% else %}
_(note file not found)_
{% endif %}

### プロダクト方針 / Product brief (product-brief.md)
{% if brief %}
{{ brief }}
{% else %}
_(no product-brief.md in this worktree)_
{% endif %}
