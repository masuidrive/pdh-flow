"""Tiny calculator. Supports add / sub / mul. Divide is a TODO."""


def add(a, b):
    return a + b


def sub(a, b):
    return a - b


def mul(a, b):
    return a * b


# TODO: implement divide(a, b) per the ticket's AC. Should return float.
# Should let ZeroDivisionError propagate (do NOT catch it here).
