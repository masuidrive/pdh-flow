/* eslint-disable */
/**
 * This file was automatically generated by json-schema-to-typescript.
 * DO NOT MODIFY IT BY HAND. Instead, modify the source JSONSchema file,
 * and run json-schema-to-typescript to regenerate this file.
 */

/**
 * Canonical runtime state. The XState snapshot is bookkeeping; if missing or corrupt, the engine rebuilds the machine from this frontmatter + judgements/ + git history. Authority order on conflict: this frontmatter > XState snapshot.
 */
export type CurrentNoteMdFrontmatter = {
  [k: string]: unknown;
} & {
  version: 1;
  /**
   * YYMMDD-HHMMSS-slug. Matches pdh-flow's existing ticket naming.
   */
  ticket_id: string;
  flow: string;
  variant: string;
  /**
   * Engine-assigned run id. Opaque format; consumers must not parse.
   */
  run_id: string;
  /**
   * Lower-case dotted path. Dots indicate parent.child relationship (e.g. parallel-group member). Underscores join words within a segment.
   */
  current_node_id: string;
  /**
   * Round counter, scoped to the active review_loop. 1 for nodes outside any loop.
   */
  round: number;
  status: "in_progress" | "needs_human" | "completed" | "aborted" | "cancelled";
  /**
   * RFC 3339 / ISO 8601 datetime, UTC preferred.
   */
  started_at: string;
  /**
   * RFC 3339 / ISO 8601 datetime, UTC preferred.
   */
  last_advanced_at: string;
  /**
   * RFC 3339 / ISO 8601 datetime, UTC preferred.
   */
  completed_at?: string;
  needs_human?: {
    reason:
      | "gate_pending"
      | "max_rounds_exceeded"
      | "explicit_pause"
      | "schema_violation"
      | "guardian_violation"
      | "preflight_failed"
      | "error";
    /**
     * Lower-case dotted path. Dots indicate parent.child relationship (e.g. parallel-group member). Underscores join words within a segment.
     */
    node_id: string;
    /**
     * RFC 3339 / ISO 8601 datetime, UTC preferred.
     */
    since: string;
    details?: string;
  };
  /**
   * Brief trail of completed nodes for fast UI rendering. Authoritative history is git log + judgements/. Entries appended on every node completion.
   */
  history?: HistoryEntry[];
};

export interface HistoryEntry {
  /**
   * Lower-case dotted path. Dots indicate parent.child relationship (e.g. parallel-group member). Underscores join words within a segment.
   */
  node_id: string;
  round: number;
  outcome: "completed" | "failed" | "approved" | "rejected" | "passed" | "repair_needed" | "aborted" | "escalated";
  /**
   * RFC 3339 / ISO 8601 datetime, UTC preferred.
   */
  at: string;
  /**
   * Hex commit sha; full or short form.
   */
  commit_sha?: string;
  summary?: string;
}
