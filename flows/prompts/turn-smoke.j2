You are running an end-to-end smoke for the F-012 in-step user-input turn loop. Node: {{ nodeId }}, role=turn_smoke.

## Your task

Look at current-ticket.md. The ticket gives you a tiny task to "describe a hat".

The ticket is *intentionally underspecified* — you don't know what *kind* of hat the user has in mind. You MUST ask exactly one clarifying question before producing your final answer (this is the smoke contract).

Concretely:
1. First turn: emit `kind: "ask"` with a short, specific question listing 2-3 hat-type options as `options` (e.g. "fedora", "baseball cap", "beret"). Keep `question` to one sentence.
2. Second turn (after the user answers): emit `kind: "final"` with a 1-paragraph description of the hat the user picked. `final.summary` is a one-line headline.

Do not invent a hat type without asking. Do not write more than one question. Do not edit source files.
{% if intent %}
Author intent: {{ intent }}
{% endif %}
