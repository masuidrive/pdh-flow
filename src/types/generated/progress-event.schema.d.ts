/* eslint-disable */
/**
 * This file was automatically generated by json-schema-to-typescript.
 * DO NOT MODIFY IT BY HAND. Instead, modify the source JSONSchema file,
 * and run json-schema-to-typescript to regenerate this file.
 */

export type Kind =
  | "run_started"
  | "run_completed"
  | "run_aborted"
  | "run_cancelled"
  | "run_paused"
  | "run_resumed"
  | "node_started"
  | "node_completed"
  | "node_failed"
  | "round_advanced"
  | "guardian_decided"
  | "gate_pending"
  | "gate_decided"
  | "lease_acquired"
  | "lease_released"
  | "interrupt_raised"
  | "interrupt_resolved"
  | "snapshot_saved"
  | "snapshot_restored"
  | "schema_violation"
  | "warning"
  | "error";

/**
 * Observability event emitted by the engine on every state change. Append-only stream at .pdh-flow/runs/<run>/events.jsonl. Consumed by Web UI live view, CLI watchers, and post-mortem inspection. Authority: zero — events are derivable from durable state.
 */
export interface ProgressEvent {
  v: 1;
  /**
   * Monotonic per-run sequence number.
   */
  seq: number;
  kind: Kind;
  /**
   * RFC 3339 / ISO 8601 datetime, UTC preferred.
   */
  at: string;
  /**
   * Engine-assigned run id. Opaque format; consumers must not parse.
   */
  run_id: string;
  /**
   * YYMMDD-HHMMSS-slug. Matches pdh-flow's existing ticket naming.
   */
  ticket_id?: string;
  /**
   * Lower-case dotted path. Dots indicate parent.child relationship (e.g. parallel-group member). Underscores join words within a segment.
   */
  node_id?: string;
  round?: number;
  /**
   * Human-readable 1-line. UI uses this verbatim in feeds.
   */
  summary?: string;
  level?: "info" | "warning" | "error";
  /**
   * Kind-specific data. Loose schema; consumers ignore unknown fields. Examples: { provider, evidence_path }, { decision, blocking_findings_count }, { released_leases }.
   */
  payload?: {};
}
