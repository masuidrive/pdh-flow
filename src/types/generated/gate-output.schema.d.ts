/* eslint-disable */
/**
 * This file was automatically generated by json-schema-to-typescript.
 * DO NOT MODIFY IT BY HAND. Instead, modify the source JSONSchema file,
 * and run json-schema-to-typescript to regenerate this file.
 */

/**
 * Result of a gate_step decision by a human (or other principal). Persisted as the durable record of the approval / rejection. Engine maps decision → transition via the gate_step's outputs map.
 */
export type GateStepOutput = {
  [k: string]: unknown;
} & {
  status: "completed" | "cancelled" | "timed_out";
  /**
   * Lower-case dotted path. Dots indicate parent.child relationship (e.g. parallel-group member). Underscores join words within a segment.
   */
  node_id: string;
  round?: number;
  decision: "approved" | "rejected" | "cancelled";
  /**
   * Username, role label, or agent identifier of who decided.
   */
  approver?: string;
  /**
   * RFC 3339 / ISO 8601 datetime, UTC preferred.
   */
  decided_at: string;
  /**
   * Optional free-form note explaining the decision. Audited but does not affect transition.
   */
  comment?: string;
  /**
   * Values submitted via the gate's form fields, keyed by field.name. Strings (text/textarea/select) or booleans (checkbox).
   */
  form_data?: {};
  /**
   * When the close_gate decision is `approved` and the upstream final_verification table contains `unverified` AC rows, each unverified row MUST have a corresponding deferral approval entry here. Engine rejects approval if rows are missing. Skipped when no unverified rows exist.
   */
  deferral_approvals?: {
    ac_item: string;
    follow_up_ticket: string;
    reason: string;
  }[];
  /**
   * When the gate-summary surfaced concerns at this gate, each concern MUST be triaged here before the engine accepts an `approved` decision. The triage record drives ticket write-backs and routing: `fix_in_this_ticket` forces the gate to reject so the implementer can address the concern in the current ticket; `accept` echoes the text into `# Out of scope`; `defer` records a follow-up_ticket pointer in `# Resolution`; `dismiss` stays in the gate-decision JSON only.
   */
  concern_triage?: {
    /**
     * The concern text as surfaced by gate-summary (verbatim or PdM-paraphrased).
     */
    concern: string;
    /**
     * `fix_in_this_ticket` = real concern, address it in the current ticket (engine rejects approve and routes back to implement with the rationale as a structured fix item); `accept` = real concern, consciously left as-is (Out of scope); `defer` = real concern, will fix in a separate follow-up ticket; `dismiss` = not a real concern / LLM false positive.
     */
    action: "fix_in_this_ticket" | "accept" | "defer" | "dismiss";
    /**
     * One-line reason. Persists to the gate decision JSON and (for fix_in_this_ticket / accept / defer) to the ticket or implementer's fix list.
     */
    rationale: string;
    /**
     * Required when action=defer. Slug of an existing or to-be-cut follow-up ticket.
     */
    follow_up_ticket?: string;
  }[];
  via?: "cli" | "web_ui" | "api" | "assist";
};
