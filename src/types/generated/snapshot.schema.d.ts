/* eslint-disable */
/**
 * This file was automatically generated by json-schema-to-typescript.
 * DO NOT MODIFY IT BY HAND. Instead, modify the source JSONSchema file,
 * and run json-schema-to-typescript to regenerate this file.
 */

/**
 * Wraps XState v5 actor.getPersistedSnapshot() with engine identity / metadata. Bookkeeping artifact for fast resume; not canonical. If missing or invalid, the engine rebuilds the machine from canonical state (note-frontmatter + judgements + git).
 */
export interface EngineSnapshot {
  version: 1;
  engine: {
    name: "pdh-flow";
    version: string;
    xstate_version?: string;
    /**
     * Hash of the compiled flat-flow used to construct the machine. Engine refuses to restore if the hash differs from the current compiled flow (= flow definition changed under us).
     */
    machine_hash?: string;
  };
  /**
   * RFC 3339 / ISO 8601 datetime, UTC preferred.
   */
  saved_at: string;
  /**
   * Engine-assigned run id. Opaque format; consumers must not parse.
   */
  run_id: string;
  /**
   * YYMMDD-HHMMSS-slug. Matches pdh-flow's existing ticket naming.
   */
  ticket_id: string;
  flow: string;
  variant: string;
  /**
   * Opaque payload from XState v5 actor.getPersistedSnapshot(). Engine does not interpret structure — passes through to createActor(machine, { snapshot }).
   */
  xstate_snapshot: {};
}
